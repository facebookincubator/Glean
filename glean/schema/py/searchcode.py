# @generated
# To regenerate this file run fbcode//glean/schema/gen/sync
from typing import Union
import json
from glean.schema.py.glean_schema_predicate import GleanSchemaPredicate


class GSSearchCodeSearchByLowerCaseScope(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.SearchByLowerCaseScope.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeSearchByLowerCaseScope":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodePythonSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.PythonSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodePythonSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeErlangSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.ErlangSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeErlangSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHsSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HsSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHsSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHackSearchByLowerCaseScope(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HackSearchByLowerCaseScope.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHackSearchByLowerCaseScope":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeCxxSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.CxxSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeCxxSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.SearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHackSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HackSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHackSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHackSearchByScopeWithName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HackSearchByScopeWithName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHackSearchByScopeWithName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeCxxSearchByLowerCaseScope(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.CxxSearchByLowerCaseScope.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeCxxSearchByLowerCaseScope":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodePythonSearchByLocalNameFact(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.PythonSearchByLocalNameFact.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodePythonSearchByLocalNameFact":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHackSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HackSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHackSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeLsifSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.LsifSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeLsifSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeRustSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.RustSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeRustSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeCxxSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.CxxSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeCxxSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodePythonSearchByNameFact(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.PythonSearchByNameFact.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodePythonSearchByNameFact":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHsSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HsSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHsSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeErlangSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.ErlangSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeErlangSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeSearchByScope(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.SearchByScope.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeSearchByScope":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeFlowSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.FlowSearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeFlowSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeFlowSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.FlowSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeFlowSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeRustSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.RustSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeRustSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeLsifSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.LsifSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeLsifSearchByName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeSearchByNameAndLanguage(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.SearchByNameAndLanguage.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeSearchByNameAndLanguage":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeCxxSearchByScope(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.CxxSearchByScope.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeCxxSearchByScope":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeSearchByLowerCaseNameAndLanguage(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.SearchByLowerCaseNameAndLanguage.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeSearchByLowerCaseNameAndLanguage":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeSearchByLowerCaseName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.SearchByLowerCaseName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeSearchByLowerCaseName":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodeHackSearchByScope(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.HackSearchByScope.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodeHackSearchByScope":
    raise Exception("this function can only be called from @angle_query")

class GSSearchCodePythonSearchByName(GleanSchemaPredicate):
  @staticmethod
  def build_angle(key: Union[str, int]) -> str:
    return f"search.code.PythonSearchByName.16 { json.dumps(key) }"

  @staticmethod
  def angle_query(*, name: str) -> "GSSearchCodePythonSearchByName":
    raise Exception("this function can only be called from @angle_query")


